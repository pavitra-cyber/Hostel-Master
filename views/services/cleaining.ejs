<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cleaning & Sanitation</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* General Page Styling */
        body {
            font-family: 'Poppins', sans-serif;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            background: linear-gradient(270deg, #6dd5ed, #f7797d);
            background-size: 400% 400%;
            animation: gradientBG 10s ease infinite;
            color: #333;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Heading Styles */
        h2 {
    background: linear-gradient(90deg, #003092, #E50046);
    color: white;
    padding: 15px;
    width: 100%;
    text-align: center;
    margin:0;
    font-size: 2.5rem;
    animation: slideFadeIn 1.2s ease-out forwards;
    transform: translateY(-50px);
    opacity: 0;
    }

    /* Animation Keyframes */
    @keyframes slideFadeIn {
        0% {
            transform: translateY(-50px);
            opacity: 0;
        }
        100% {
            transform: translateY(0px);
            opacity: 1;
        }
    }


        p {
            margin-top: 20px;
            margin-bottom: 10px;
            font-size: 1.3rem;
            animation: fadeIn 2s ease forwards;
        }

        h3 {
            color: white;
            margin-top: 30px;
            font-size: 2rem;
            animation: fadeIn 2.5s ease forwards;
        }

        /* Image Styling */
        .cleaning_img img {
            margin-top: 20px;
            height: 50vh;
            max-width: 90%;
            border-radius: 20px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.3);
            transition: transform 0.5s;
        }

        .cleaning_img img:hover {
            transform: scale(1.05);
        }

        /* Button Styling */
        .btn {
    display: inline-block;
    padding: 12px 25px;
    background: linear-gradient(90deg, #E50046, #003092);
    color: white;
    text-decoration: none;
    border: none;
    border-radius: 50px;
    font-size: 1.2rem;
    font-weight: 600;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
    transition: all 0.4s ease;
    cursor: pointer;
    margin: 20px 0;
}

/* Hover effect */
.btn:hover {
    background: linear-gradient(90deg, #003092, #E50046);
    box-shadow: 0 12px 20px rgba(0, 0, 0, 0.3);
    transform: translateY(-3px) scale(1.05);
}

        /* Issues Container */
        .issues-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 30px auto;
            max-width: 1200px;
        }

        /* Individual Issue Card */
        .issue-card {
    background: linear-gradient(145deg, #ffffff, #f0f0f5);
    padding: 20px;
    border-radius: 20px;
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.15);
    width: 300px;
    text-align: left;
    transition: transform 0.4s, box-shadow 0.4s;
    animation: fadeInUp 1.5s ease forwards;
    position: relative;
    overflow: hidden;
    border: 1px solid #e6e6e6;
}

.issue-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.25);
    background: linear-gradient(145deg, #e6f0ff, #ffe6f0);
}

/* Card Heading */
.issue-card h4 {
    margin: 0;
    color: #003092;
    font-size: 1.4rem;
    margin-bottom: 8px;
}

/* Card Description */
.issue-card p {
    color: #444;
    font-size: 1rem;
    margin-top: 10px;
    line-height: 1.5;
}

/* Status Badge */
.issue-card span {
    display: inline-block;
    margin-top: 12px;
    font-weight: bold;
    color: #28a745;
    background-color: #e6ffe6;
    padding: 6px 14px;
    border-radius: 30px;
    font-size: 0.9rem;
}

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .card-buttons {
    margin-top: 15px;
    display: flex;
    justify-content: space-between;
}

.btn-sm {
    padding: 8px 16px;
    font-size: 0.9rem;
    font-weight: 600;
    border: none;
    border-radius: 20px;
    cursor: pointer;
    text-decoration: none;
    color: white;
    transition: background 0.3s ease;
}

.show-btn {
    background: #007bff;
}

.show-btn:hover {
    background: #0056b3;
}

.delete-btn {
    background: #dc3545;
}

.delete-btn:hover {
    background: #a71d2a;
}

/* Styling for Images Button */
.image-btn {
    background-color: #A0C878; /* Blue */
    color: white;
    padding: 8px 15px;
    border-radius: 4px;
    text-decoration: none;
    font-size: 14px;
    font-weight: bold;
    display: inline-block;
    transition: background-color 0.3s ease, transform 0.3s ease;
}

.image-btn:hover {
    background-color: #A4B465; /* Darker blue on hover */
    transform: scale(1.05); /* Slight scaling effect */
}

.image-btn:active {
    background-color: #626F47; /* Even darker blue on click */
    transform: scale(1); /* Reset scaling on click */
}
    </style>
</head>
<body>
    <h2>Cleaning & Sanitation Services</h2>
    <p>Regular hostel cleaning, sanitization, and waste disposal services.</p>
    <div class="cleaning_img">
        <img src="/images/cleaning1.png" alt="cleaning image">
    </div>
    <div class="btn">
    <h3>Cleaning Issues Reported</h3>
    </div>
    <div class="issues-container">
        <% issues.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)); %>
        <% issues.forEach(issue => { %>
            <div class="issue-card">
                <h4>Room <%= issue.room %> - <%= issue.name %></h4>
                <p>Description: <%= issue.description %></p>
                <span>Status: <%= issue.status %></span>

                <div class="card-buttons">
                    <a href="/issues/<%= issue._id %>" class="btn-sm show-btn">Show</a>
                    <form action="/issues/<%= issue._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" class="btn-sm delete-btn">Delete</button>
                    </form>

                    <a href="/issues/<%= issue._id %>/images" class="btn-sm image-btn">Images</a>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>
