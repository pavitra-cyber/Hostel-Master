<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plumbing & Maintenance</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding-top: 90px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            background: linear-gradient(135deg, #00c9a7, #92fe9d);
            background-size: 400% 400%;
            animation: gradientBG 10s ease infinite;
            color: #222;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        h2 {
            background: linear-gradient(90deg, #ff6f61, #00c9a7);
            color: #fff;
            padding: 15px;
            width: 100%;
            text-align: center;
            margin: 0;
            font-size: 2.5rem;
            animation: slideFadeIn 1.2s ease-out forwards;
            transform: translateY(-50px);
            opacity: 0;
        }

        @keyframes slideFadeIn {
            0% { transform: translateY(-50px); opacity: 0; }
            100% { transform: translateY(0); opacity: 1; }
        }

        p {
            margin-top: 20px;
            margin-bottom: 10px;
            font-size: 1.3rem;
            animation: fadeIn 2s ease forwards;
        }

        h3 {
            color: #00796b;
            margin-top: 30px;
            font-size: 2rem;
            animation: fadeIn 2.5s ease forwards;
        }

        .cleaning_img img {
            margin-top: 20px;
            height: 50vh;
            max-width: 90%;
            border-radius: 20px;
            box-shadow: 0 8px 25px rgba(0, 180, 158, 0.4);
            transition: transform 0.5s;
        }

        .cleaning_img img:hover {
            transform: scale(1.05);
        }

        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(90deg, #00c9a7, #ff6f61);
            color: #fff;
            text-decoration: none;
            border: none;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 600;
            box-shadow: 0 10px 20px rgba(0, 180, 158, 0.3);
            transition: all 0.4s ease;
            cursor: pointer;
            margin: 25px 0;
        }

        .btn:hover {
            background: linear-gradient(90deg, #ff6f61, #00c9a7);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 14px 28px rgba(255, 111, 97, 0.4);
        }

        .issues-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 30px auto;
            max-width: 1200px;
        }

        .issue-card {
            background: linear-gradient(145deg, #ffffff, #eafaf7);
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.15);
            width: 300px;
            text-align: left;
            transition: transform 0.4s, box-shadow 0.4s;
            animation: fadeInUp 1.5s ease forwards;
            position: relative;
            border: 1px solid #d9f8f0;
        }

        .issue-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 14px 24px rgba(0, 180, 158, 0.4);
            background: linear-gradient(145deg, #e0f7f4, #fff4f3);
        }

        .issue-card h4 {
            margin: 0;
            color: #00796b;
            font-size: 1.4rem;
            margin-bottom: 8px;
        }

        .issue-card p {
            color: #444;
            font-size: 1rem;
            margin-top: 10px;
            line-height: 1.5;
        }

        .issue-card span {
            display: inline-block;
            margin-top: 12px;
            font-weight: bold;
            color: #388e3c;
            background-color: #d1f7d4;
            padding: 6px 14px;
            border-radius: 30px;
            font-size: 0.9rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 600px) {
            .issue-card {
                width: 90%;
            }
        }

        .card-buttons {
    margin-top: 15px;
    display: flex;
    justify-content: space-between;
    gap: 10px;
    flex-wrap: wrap;
}

.card-buttons a,
.card-buttons button {
    flex: 1 1 30%;
    display: inline-block;
    text-align: center;
    padding: 10px 0;
    border-radius: 30px;
    font-weight: 600;
    font-size: 0.95rem;
    color: #fff;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 5px 15px rgba(0, 255, 180, 0.2);
    min-width: 90px;
}

/* Fix for button inside form */
.card-buttons form {
    flex: 1 1 30%;
    margin: 0;
}

/* Specific Button Styles */
.show-btn {
    background: linear-gradient(90deg, #FFB433, #3E3F5B);
    color: #000;
}

.show-btn:hover {
    background: linear-gradient(90deg, #DD88CF, #4B164C);
    transform: translateY(-2px) scale(1.05);
}

.delete-btn {
    background: linear-gradient(90deg, #FB2576, #1B1A55);
}

.delete-btn:hover {
    background: linear-gradient(90deg, #123524, #3E7B27);
    transform: translateY(-2px) scale(1.05);
}

.image-btn {
    background: linear-gradient(90deg,  #FF204E,#DCD7C9);
}

.image-btn:hover {
    background: linear-gradient(90deg, #641B2E, #3A0519);
    transform: translateY(-2px) scale(1.05);
}
    </style>
</head>
<body>
    <%- include("../include/navbar.ejs")%>
    <h2>Plumbing & Maintenance Services</h2>
    <p>Report issues related to taps, leaks, drainage, and other plumbing faults.</p>
    <div class="cleaning_img">
        <img src="/images/plumbing1.png" alt="Plumber image">
    </div>

    <div class="btn">
    <h3>Plumbing Issues Reported</h3>
    </div>
    <div class="issues-container">
        <% issues.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)); %>
        <% issues.forEach(issue => { %>
            <div class="issue-card">
                <h4>Room <%= issue.room %> - <%= issue.name %></h4>
                <p>Description: <%= issue.description %></p>
                <span>Status: <%= issue.status %></span>

                <div class="card-buttons">
                    <a href="/issues/<%= issue._id %>" class="btn-sm show-btn">Show</a>
                    <form action="/issues/<%= issue._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" class="btn-sm delete-btn">Delete</button>
                    </form>
                    <a href="/issues/<%= issue._id %>/images" class="btn-sm image-btn">Images</a>
                </div>                
            </div>
        <% }); %>
    </div>
</body>
</html>
