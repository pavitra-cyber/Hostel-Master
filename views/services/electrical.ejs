<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Electrical & Maintenance</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        /* General Page Styling */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding-top: 90px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            background: linear-gradient(270deg, #0f2027, #203a43, #2c5364);
            background-size: 400% 400%;
            animation: gradientBG 12s ease infinite;
            color: #eee;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Heading Styles */
        h2 {
            background: linear-gradient(90deg, #2dfc52, #00c9ff);
            color: #000;
            padding: 15px;
            width: 100%;
            text-align: center;
            margin: 0;
            font-size: 2.5rem;
            animation: slideFadeIn 1.2s ease-out forwards;
            transform: translateY(-50px);
            opacity: 0;
        }

        @keyframes slideFadeIn {
            0% { transform: translateY(-50px); opacity: 0; }
            100% { transform: translateY(0); opacity: 1; }
        }

        p {
            margin-top: 20px;
            margin-bottom: 10px;
            font-size: 1.3rem;
            animation: fadeIn 2s ease forwards;
        }

        h3 {
            color: black;
            margin-top: 30px;
            font-size: 2rem;
            animation: fadeIn 2.5s ease forwards;
        }

        /* Image Styling */
        .cleaning_img img {
            margin-top: 20px;
            height: 50vh;
            max-width: 90%;
            border-radius: 20px;
            box-shadow: 0 8px 25px rgba(0, 255, 180, 0.3);
            transition: transform 0.5s;
        }

        .cleaning_img img:hover {
            transform: scale(1.05);
        }

        /* Button Styling */
        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(90deg, #00c9ff, #2dfc52);
            color: #000;
            text-decoration: none;
            border: none;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 600;
            box-shadow: 0 10px 20px rgba(0, 255, 180, 0.3);
            transition: all 0.4s ease;
            cursor: pointer;
            margin: 25px 0;
        }

        .btn:hover {
            background: linear-gradient(90deg, #2dfc52, #00c9ff);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 14px 28px rgba(0, 255, 180, 0.4);
        }

        /* Issues Container */
        .issues-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 30px auto;
            max-width: 1200px;
        }

        /* Individual Issue Card */
        .issue-card {
            background: linear-gradient(145deg, #1e293b, #334155);
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.25);
            width: 300px;
            text-align: left;
            transition: transform 0.4s, box-shadow 0.4s;
            animation: fadeInUp 1.5s ease forwards;
            border: 1px solid #2dfc52;
            color: #f1f1f1;
        }

        .issue-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 14px 24px rgba(0, 255, 180, 0.4);
            background: linear-gradient(145deg, #2c3e50, #1a252f);
        }

        .issue-card h4 {
            margin: 0;
            color: #2dfc52;
            font-size: 1.4rem;
            margin-bottom: 8px;
        }

        .issue-card p {
            color: #ddd;
            font-size: 1rem;
            margin-top: 10px;
            line-height: 1.5;
        }

        .issue-card span {
            display: inline-block;
            margin-top: 12px;
            font-weight: bold;
            color: #00ff90;
            background-color: #003b2e;
            padding: 6px 14px;
            border-radius: 30px;
            font-size: 0.9rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 600px) {
            .issue-card {
                width: 90%;
            }
        }
        .card-buttons {
    margin-top: 15px;
    display: flex;
    justify-content: space-between;
    gap: 10px;
    flex-wrap: wrap;
}

.card-buttons a,
.card-buttons button {
    flex: 1 1 30%;
    display: inline-block;
    text-align: center;
    padding: 10px 0;
    border-radius: 30px;
    font-weight: 600;
    font-size: 0.95rem;
    color: #fff;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 5px 15px rgba(0, 255, 180, 0.2);
    min-width: 90px;
}

/* Fix for button inside form */
.card-buttons form {
    flex: 1 1 30%;
    margin: 0;
}

/* Specific Button Styles */
.show-btn {
    background: linear-gradient(90deg, #00c9ff, #2dfc52);
    color: #000;
}

.show-btn:hover {
    background: linear-gradient(90deg, #2dfc52, #00c9ff);
    transform: translateY(-2px) scale(1.05);
}

.delete-btn {
    background: linear-gradient(90deg, #ff4b2b, #ff416c);
}

.delete-btn:hover {
    background: linear-gradient(90deg, #ff416c, #ff4b2b);
    transform: translateY(-2px) scale(1.05);
}

.image-btn {
    background: linear-gradient(90deg, #8e2de2, #4a00e0);
}

.image-btn:hover {
    background: linear-gradient(90deg, #4a00e0, #8e2de2);
    transform: translateY(-2px) scale(1.05);
}

    </style>
</head>
<body>
    <%- include("../include/navbar.ejs")%>
    <h2>Electrical & Maintenance Services</h2>
    <p>Report issues related to wiring, lights, power sockets, and other electrical faults.</p>

    <div class="cleaning_img">
        <img src="/images/electrical1.png" alt="Electrician image">
    </div>

    <div class="btn">
        <h3>Electricity Issues Reported</h3>
    </div>

    <div class="issues-container">
        <% issues.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)); %>
        <% issues.forEach(issue => { %>
            <div class="issue-card">
                <h4>Room <%= issue.room %> - <%= issue.name %></h4>
                <p>Description: <%= issue.description %></p>
                <span>Status: <%= issue.status %></span>

                <div class="card-buttons">
                    <a href="/issues/<%= issue._id %>" class="btn-sm show-btn">Show</a>
                    <form action="/issues/<%= issue._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" class="btn-sm delete-btn">Delete</button>
                    </form>
                    <a href="/issues/<%= issue._id %>/images" class="btn-sm image-btn">Images</a>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>
